apiVersion: v1
kind: ConfigMap
metadata:
  name: httpserver
  labels:
    app: httpserver
data:
  CERTIFICATE_FILE: "/usr/secrets/httpserver/mycert.crt"
  CERTIFICATE_KEY: "/usr/secrets/httpserver/mycert.key"